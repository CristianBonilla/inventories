import { CollapseSidebarDirective } from './collapse-sidebar.directive';

describe('CollapseSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
